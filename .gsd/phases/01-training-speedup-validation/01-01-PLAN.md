# S01: Benchmark matrix and reproducibility

**Milestone:** M001
**Slice:** S01

**Goal:** Create a reproducible, resource-aware benchmark matrix and evidence schema before implementing optimizations.
**Demo:** A committed benchmark plan generates paired baseline and candidate runs for nuisance and no-nuisance modes at 100k and 1m events, with resource-aware scheduler metadata.

## Must-Haves

- Baseline benchmark definitions cover no-nuisance and each supported nuisance mode at 100k and 1m events, use paired seeds, exclude batch-size/data-loader hypotheses, and record CPU/GPU concurrency and timing metadata.

## Proof Level

- This slice proves: automated

## Integration Closure

Exercise config generation, local smoke execution, and PBS submission rendering together.

## Verification

- Adds structured benchmark metadata and failure diagnostics.

<tasks>
- [ ] **T01**: Inventory training modes and entry points _(2h)_
  Map no-nuisance and supported nuisance configurations, event-size configuration keys, single-batch execution, checkpoint restore, prediction, and plotting callers. Record affected files and define the exact four-cell workload matrix.
  - Files: `train`, `neural_networks`, `configs`, `test`
  - Verify: Repository-wide search plus focused configuration validation tests.
- [ ] **T02**: Define benchmark result schema _(3h)_
  Add one canonical schema/helper for seed, event scale, nuisance mode, resource request, effective concurrency, PBS identifiers, wall time, training time, memory, checkpoint metrics, profiler paths, and equivalence verdict. Avoid duplicate definitions in submission and analysis code.
  - Files: `frame`, `configs/experiments`, `test`
  - Verify: python -m pytest focused benchmark metadata tests
- [ ] **T03**: Create paired baseline benchmark packs _(3h)_
  Create frozen baseline/candidate-compatible packs for 100k and 1m events, no nuisance and each supported nuisance mode, with explicit seeds and single-batch settings. Add CPU and GPU resource variants with documented concurrency caps.
  - Files: `configs/experiments`, `README.md`
  - Verify: Config creation and cross-validation for every matrix cell; generated command dry run.
- [ ] **T04**: Add equivalence and acceptance checks _(4h)_
  Implement deterministic comparison tooling for model outputs, losses, predictions, checkpoint restore results, and relevant histories. Define timing acceptance thresholds and fail-on-any-cell regression logic.
  - Files: `frame`, `test`, `configs/experiments`
  - Verify: Focused tests covering equal outputs, mismatch, missing artifact, and failed-job cases.
</tasks>

## Files Likely Touched

- train
- neural_networks
- configs
- test
- frame
- configs/experiments
- README.md
<!-- gsd:state-version=2:0 -->
