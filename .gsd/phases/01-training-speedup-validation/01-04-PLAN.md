# S04: Checkpoint serialization optimization

**Milestone:** M001
**Slice:** S04

**Goal:** Test checkpoint serialization improvements while preserving checkpoint contents, update order, and continuation semantics.
**Demo:** A checkpoint mechanism with unchanged checkpoint contents and update order is compared against baseline under the full matrix and accepted only if checkpoint cost falls without training regression.

## Must-Haves

- The checkpoint candidate passes the full nuisance/no-nuisance and 100k/1m matrix, restores correctly, and improves or preserves total runtime without artifact races or loss.

## Proof Level

- This slice proves: automated

## Integration Closure

Verify fresh training, interruption, continuation, checkpoint restore, predictions, and plotting integration on applicable CPU/GPU paths.

## Verification

- Record checkpoint count, serialization duration, flush state, artifact paths, and continuation failures.

<tasks>
- [ ] **T01**: Measure checkpoint cost and semantics _(4h)_
  Instrument checkpoint creation and serialization duration, file sizes, flush behavior, and continuation boundaries for current training. Confirm exact checkpoint contents and update ordering.
  - Files: `frame`, `neural_networks`, `train`, `test`
  - Verify: Checkpoint restore and interrupted-continuation tests pass with artifact integrity checks.
- [ ] **T02**: Implement safe checkpoint optimization _(6h)_
  Implement one candidate serialization or scheduling change only if it preserves checkpoint contents, ordering, failure visibility, and continuation behavior. Do not alter batch structure or optimizer mathematics.
  - Files: `frame`, `train`, `test`
  - Verify: Checkpoint content comparison, restore, continuation, failure-path, and timing tests.
- [ ] **T03**: Run checkpoint candidate matrix _(1d)_
  Run paired CPU/GPU matrix cells at 100k and 1m events for no nuisance and each nuisance mode, reducing GPU concurrency to account for requested GPUs. Capture serialization and total timings plus equivalence evidence.
  - Files: `configs/experiments/training-speedups`
  - Verify: Acceptance evaluator produces pass/reject verdict for every required cell.
</tasks>

## Files Likely Touched

- frame
- neural_networks
- train
- test
- configs/experiments/training-speedups
<!-- gsd:state-version=5:0 -->
