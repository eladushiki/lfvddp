# S03: Tensor allocation and copy reduction

**Milestone:** M001
**Slice:** S03

**Goal:** Test reduction of temporary tensor allocation, indexing, and copy overhead in the single-batch hot path.
**Demo:** The allocation/copy candidate completes the full matrix without regression and reports memory and operator timing evidence.

## Must-Haves

- The candidate passes all nuisance/no-nuisance and 100k/1m cells, preserves equivalence, and does not exceed the baseline memory threshold.

## Proof Level

- This slice proves: automated

## Integration Closure

Verify CPU/GPU training, checkpoint restore, predictions, nuisance prediction, and plotting integration.

## Verification

- Record allocation/copy operator timings, peak memory, device, and failures.

<tasks>
- [ ] **T01**: Inventory allocation and copy costs _(3h)_
  Analyze full-window CPU and GPU profiles and callers for temporary allocations, index_add, copy, and conversion operations. Exclude batching and data-loader work because all training uses one batch.
  - Files: `neural_networks`, `data_tools`, `test`
  - Verify: Profile evidence and repository-wide caller inventory.
- [ ] **T02**: Implement allocation reduction _(6h)_
  Reuse safe buffers or fuse equivalent operations without changing operation order, dtype, device semantics, or gradients. Keep one canonical implementation and support an explicit comparison mode.
  - Files: `neural_networks`, `data_tools`, `frame`
  - Verify: Unit, gradient, deterministic equivalence, checkpoint restore, and prediction tests.
- [ ] **T03**: Run allocation candidate matrix _(1d)_
  Run paired CPU/GPU matrix cells at 100k and 1m events for no nuisance and each nuisance mode, with GPU concurrency reduced according to allocation. Capture timing, peak memory, and equivalence evidence.
  - Files: `configs/experiments/training-speedups`
  - Verify: Acceptance evaluator produces pass/reject verdict for every matrix cell.
</tasks>

## Files Likely Touched

- neural_networks
- data_tools
- test
- frame
- configs/experiments/training-speedups
<!-- gsd:state-version=4:0 -->
