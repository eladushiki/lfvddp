# S02: Invariant computation reduction

**Milestone:** M001
**Slice:** S02

**Goal:** Test removal or caching of parameter-independent computations in the single-batch training hot path.
**Demo:** The invariant-computation candidate runs through the full benchmark matrix and is accepted or rejected using paired timing and output checks.

## Must-Haves

- The candidate passes the complete matrix and equivalence checks, with no nuisance/no-nuisance regression at either event scale.

## Proof Level

- This slice proves: automated

## Integration Closure

Verify all affected training and prediction entry points plus checkpoint restore and plotting integration.

## Verification

- Record cache hit counts and per-stage timings.

<tasks>
- [ ] **T01**: Locate invariant hot-path work _(3h)_
  Use full-window profiler traces and call inventory to identify computations independent of trainable parameters, nuisance values, and batch contents. Exclude data-loader and batch-size work because training is single-batch.
  - Files: `neural_networks`, `train`, `test`
  - Verify: Profiler/call inventory reviewed against training and prediction callers.
- [ ] **T02**: Implement invariant computation reuse _(5h)_
  Introduce one canonical cache or precomputation path for validated invariant tensors/indices/constants, preserving dtype, device, update order, and mathematical operations. Add opt-in comparison instrumentation.
  - Files: `neural_networks`, `data_tools`, `frame`
  - Verify: Focused unit tests and deterministic output comparison across cache enabled/disabled modes.
- [ ] **T03**: Run invariant candidate matrix _(1d)_
  Run paired CPU/GPU benchmark cells for no nuisance and each nuisance mode at 100k and 1m events, respecting reduced GPU concurrency. Collect timing, memory, equivalence, and failure evidence.
  - Files: `configs/experiments/training-speedups`
  - Verify: Benchmark acceptance evaluator passes or produces a rejected-candidate report.
</tasks>

## Files Likely Touched

- neural_networks
- train
- test
- data_tools
- frame
- configs/experiments/training-speedups
<!-- gsd:state-version=3:0 -->
